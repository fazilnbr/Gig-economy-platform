For init swag in the directory
    swag init --parseDependency --parseInternal --parseDepth 1 -md ./documentation -o ./docs

For swag not found 
    PATH="$GOPATH/bin:$PATH"
    export GOPATH="$HOME/go"
    PATH="$GOPATH/bin:$PATH"